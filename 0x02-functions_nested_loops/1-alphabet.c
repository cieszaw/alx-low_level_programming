#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 * print_alphabet - prints the english alphabet from a-z.
 * Return: Nothing
 */
int main(void) {
void print_alphabet(void) {
    char c;
    for (c = 'a'; c <= 'z'; c++) {
       putchar(c);
    }
    putchar('\n');
}
}
